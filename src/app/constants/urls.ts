import {environment} from "../../environments/environment";

const {API} = environment;

export const urls = {
  cars: `${API}/cars`,
  auto_parks: `${API}/autoparks`,
  auth: `${API}/auth`,
  users: `${API}/users`,


}
